application:
  exe_name: esmx_mpas
  cmake_build_args: -DCMAKE_PREFIX_PATH=${NCAR_LDFLAGS_PARALLELIO}
  link_libraries: piof, pioc

components:

  mpas_atmosphere:
    build_type: none
    libraries: mpas_nuopc
    install_prefix: ../../..
    fort_module: mpas_nuopc_atm.mod
